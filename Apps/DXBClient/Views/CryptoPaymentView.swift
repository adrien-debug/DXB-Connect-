import SwiftUI

struct CryptoPaymentView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Crypto Payment",
            subtitle: "Placeholder — paiement crypto à reconstruire si conservé."
        )
    }
}

#Preview {
    CryptoPaymentView()
}

